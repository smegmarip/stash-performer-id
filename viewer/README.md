# viewer

Standalone containerized React app — the **Step 1** UI (name-record management: harvest
review, dedupe, `valid` triage, name→asset activation/cascade, direct input). Talks to the
metadata-provider service's name-DB API.

Built in **Phase 2** (see `../docs/IMPLEMENTATION_PLAN.md`). Placeholder for now.
